# Docker Compose env. Copy to .env and adjust.
# Used by: docker compose (postgres, python-api, rust-api, hono-api)

POSTGRES_USER=starter
POSTGRES_PASSWORD=starter
POSTGRES_DB=starter

SECRET_KEY=change-me-in-production-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: for building rust-api image (postgres must be reachable during build).
# Uncomment and set to your postgres URL so the build container can run SQLx compile checks.
# With postgres running on host: postgresql://starter:starter@host.docker.internal:5432/starter
# On Linux with host network build: postgresql://starter:starter@127.0.0.1:5432/starter
# DATABASE_URL=postgresql://starter:starter@host.docker.internal:5432/starter
