---
title: Button
description: The shadcn Button, copied verbatim as the Rabtx starting point.
---

```tsx
import { Button } from "@rabtx/ui/button";

<Button>Save changes</Button>
```

This is the shadcn Button with nothing changed. It is the baseline the Rabtx materials get
built on, one step at a time, so every later change is visible as a difference from here.

## Variants

<Preview>
	<Button>Default</Button>
	<Button variant="outline">Outline</Button>
	<Button variant="secondary">Secondary</Button>
	<Button variant="ghost">Ghost</Button>
	<Button variant="destructive">Destructive</Button>
	<Button variant="link">Link</Button>
</Preview>

## Sizes

<Preview>
	<Button size="xs">Extra small</Button>
	<Button size="sm">Small</Button>
	<Button size="default">Default</Button>
	<Button size="lg">Large</Button>
</Preview>

## Disabled

<Preview>
	<Button disabled>Default</Button>
	<Button variant="outline" disabled>Outline</Button>
</Preview>
