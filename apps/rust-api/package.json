{
	"name": "rust-api",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "cargo run",
		"build": "cargo build --release",
		"start": "cargo run --release",
		"lint": "cargo clippy -- -D warnings",
		"format": "cargo fmt",
		"test": "cargo test"
	}
}
