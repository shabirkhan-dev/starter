{
	"name": "pwa",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run build:app && bun run build:sw && bun run scripts/copy-assets.ts",
		"build:app": "bun build src/main.ts --outfile=dist/main.js --minify --target=browser",
		"build:sw": "bun build src/sw.ts --outfile=dist/sw.js --minify --target=browser",
		"dev": "bun --hot src/dev-server.ts",
		"start": "bun src/server.ts",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.sw.json"
	},
	"devDependencies": {
		"@starter/typescript-config": "workspace:*",
		"@types/bun": "^1.3.5",
		"@types/node": "^25.5.0",
		"typescript": "^6.0.2"
	}
}
