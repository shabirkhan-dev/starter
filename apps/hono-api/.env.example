#! /bin/sh

# Copy to .env and set values. Do not commit .env.
# For Docker Compose, DATABASE_URL is set by the compose file.

DATABASE_URL=postgresql://user:password@localhost:5432/hono
PORT=3000
HOST=0.0.0.0
APP_NAME=hono-api
APP_VERSION=0.0.0
NODE_ENV=development
APP_ORIGIN=http://localhost:3000

# Auth (JWT). Required for login/refresh.
JWT_SECRET=your-access-token-secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=30d

# Optional: email (Resend) for verification and password reset
# MAILER_SENDER=onboarding@resend.dev
# RESEND_API_KEY=
