// Prisma schema – no models yet. Add tables when ready.
// Connection URL: prisma.config.ts (Migrate) and PrismaClient adapter (runtime).

generator client {
	provider = "prisma-client"
	output   = "../src/generated/prisma"
}

datasource db {
	provider = "postgresql"
}

// Add model blocks here when you add tables.
