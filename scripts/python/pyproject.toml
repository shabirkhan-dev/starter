[project]
name = "personal-os-scripts"
version = "0.1.0"
description = "Python scripts for personal-os"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.format]
quote-style = "double"

[tool.black]
line-length = 100
target-version = ["py311"]
