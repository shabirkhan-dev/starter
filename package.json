{
  "name": "starter",
  "version": "0.1.0",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "packageManager": "bun@1.3.11",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint && bun run scripts:lint",
    "lint:fix": "turbo run lint:fix",
    "format": "biome format --write . && bun run scripts:format && (cd apps/rust && cargo fmt) && (cd apps/c && bun run format)",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test && bun run test:scripts",
    "test:scripts": "bash scripts/bash/tests/main.test.sh && (command -v lua >/dev/null 2>&1 && lua scripts/lua/tests/main_test.lua || lua5.4 scripts/lua/tests/main_test.lua) && python -m unittest scripts/python/tests/test_main.py",
    "test:e2e:web": "cd apps/web && bun run test:e2e",
    "scripts:lint": "shellcheck scripts/bash/*.sh scripts/git-hooks/*.sh && (luacheck scripts/lua 2>/dev/null || true) && (ruff check scripts/python 2>/dev/null || true)",
    "scripts:format": "shfmt -i 4 -w scripts/bash/*.sh scripts/git-hooks/*.sh && (stylua scripts/lua 2>/dev/null || true) && (ruff format scripts/python 2>/dev/null || true)",
    "scripts:run": "bash scripts/bash/main.sh",
    "scripts:run:bash": "bash scripts/bash/main.sh",
    "scripts:run:lua": "lua scripts/lua/main.lua",
    "scripts:run:python": "python scripts/python/main.py",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "lefthook": "^2.1.4",
    "turbo": "^2.9.3"
  },
  "ignoreScripts": ["sharp", "unrs-resolver"],
  "trustedDependencies": ["sharp", "unrs-resolver"]
}
